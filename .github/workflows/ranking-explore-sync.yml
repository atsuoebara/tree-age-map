name: Verify EXPLORE regions (global)
on:
  workflow_dispatch:
    inputs:
      year:
        description: 'Year to verify'
        required: true
        default: '2026'
# Manual only until first successful validation; enable schedule afterward.
permissions:
  contents: read
concurrency:
  group: ranking-explore-sync
  cancel-in-progress: false
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Check geometry and supported boundary files
        run: node scripts/sync-ranking-explore.mjs --self-test
      - name: Import opted-in GPS regions worldwide
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          EXPLORE_YEAR: ${{ inputs.year || '2026' }}
        run: node scripts/sync-ranking-explore.mjs
